# =============================================================================
# SAIL CRM Engine - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control. It contains secrets.
# =============================================================================

# --- Zoho Bigin CRM ---
# Get your API token from: https://accounts.zoho.com/developerconsole
# Or use the Zoho MCP server to generate one.
ZOHO_API_TOKEN=
ZOHO_BIGIN_BASE_URL=https://www.zohoapis.com/bigin/v2

# --- Google Places API ---
# Used for searching businesses by type and location.
# Get a key at: https://console.cloud.google.com/apis/credentials
# Enable "Places API" and "Places API (New)" in your project.
# Free tier: $200/month credit (~5,000 searches)
GOOGLE_PLACES_API_KEY=

# --- Hunter.io ---
# Used for finding email addresses associated with a domain.
# Sign up at: https://hunter.io (free tier: 25 searches/month)
HUNTER_API_KEY=

# --- Apollo.io ---
# Used for finding contact info and company data.
# Sign up at: https://www.apollo.io (free tier: 10,000 records/month)
APOLLO_API_KEY=

# --- Search Configuration ---
# Default search location (city, state)
DEFAULT_SEARCH_LOCATION=San Antonio, TX
# Search radius in meters (default: 40000 = ~25 miles)
DEFAULT_SEARCH_RADIUS=40000
